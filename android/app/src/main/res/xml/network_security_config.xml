<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- The School API currently only serves plain HTTP. Cleartext is
         allowed only for that host, not for the app as a whole. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">177.44.248.76</domain>
    </domain-config>
</network-security-config>
